import React, {FC} from 'react';

import './not.found.modules.page.css'

const NotFoundPage:FC = () => {
    return (
        <div className={'not-found-page'}>
            Sorry, this page not found :(
        </div>
    );
};

export default NotFoundPage;